<div id="<%= dom_id post %>">
  <p>
    <strong>User:</strong>
    <%= post.user %>
  </p>
  <p>
    <%= image_tag(post.image.preview.url, alt: 'Image') if post.image? %>
  </p>
  <p>
    <em><%= post.caption %></em>
    <strong> ~<%= link_to post.user, post.user %></strong>
  </p> 
  
 

</div>

